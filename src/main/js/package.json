{
  "scripts": {
    "start": "snowpack dev",
    "build": "snowpack build",
    "deploy": "cp build/src/*.js ../resources/static/js && cp -r build/_snowpack ../resources/static/",
    "test": "echo \"This template does not include a test runner by default.\" && exit 1",
    "format": "prettier --write \"src/**/*.{ts,js}\"",
    "lint": "prettier --check \"src/**/*.{ts,js}\""
  },
  "dependencies": {
    "@hotwired/turbo": "^7.1.0",
    "stimulus": "^3.1.0"
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "snowpack": "^3.8.8"
  }
}
